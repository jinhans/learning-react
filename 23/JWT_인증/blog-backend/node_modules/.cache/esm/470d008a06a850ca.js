let Koa,Router,bodyParser,mongoose,serve,path,send,api,jwtMiddleware;_06a‍.w("koa",[["default",["Koa"],function(v){Koa=v}]]);_06a‍.w("koa-router",[["default",["Router"],function(v){Router=v}]]);_06a‍.w("koa-bodyparser",[["default",["bodyParser"],function(v){bodyParser=v}]]);_06a‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);_06a‍.w("koa-static",[["default",["serve"],function(v){serve=v}]]);_06a‍.w("path",[["default",["path"],function(v){path=v}]]);_06a‍.w("koa-send",[["default",["send"],function(v){send=v}]]);_06a‍.w("./api",[["default",["api"],function(v){api=v}]]);_06a‍.w("./lib/jwtMiddleware",[["default",["jwtMiddleware"],function(v){jwtMiddleware=v}]]);require('dotenv').config();











const { PORT, MONGO_URI } = process.env;

mongoose
  .connect(MONGO_URI)
  .then(() => {
    console.log('Connected to MongoDB');
  })
  .catch((e) => {
    _06a‍.g.console.error(e);
  });

const app = new Koa();
const router = new Router();

router.use('/api', api.routes());

app.use(bodyParser());
app.use(jwtMiddleware);

app.use(router.routes()).use(router.allowedMethods());

const buildDirectory = path.resolve(__dirname, '../../blog-frontend/build');
app.use(serve(buildDirectory));
app.use(async (ctx) => {
  if (ctx.status === 404 && ctx.path.indexOf('/api') !== 0) {
    await send(ctx, 'index.html', { root: buildDirectory });
  }
});

const port = PORT || 4000;
app.listen(port, () => {
  _06a‍.g.console.log('Listening to port %d', port);
});
