let Koa,Router,bodyParser,mongoose,api,jwtMiddleware;_6ee‍.w("koa",[["default",["Koa"],function(v){Koa=v}]]);_6ee‍.w("koa-router",[["default",["Router"],function(v){Router=v}]]);_6ee‍.w("koa-bodyparser",[["default",["bodyParser"],function(v){bodyParser=v}]]);_6ee‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);_6ee‍.w("./api",[["default",["api"],function(v){api=v}]]);_6ee‍.w("./lib/jwtMiddleware",[["default",["jwtMiddleware"],function(v){jwtMiddleware=v}]]);require('dotenv').config();








const { PORT, MONGO_URI } = process.env;

mongoose
  .connect(MONGO_URI)
  .then(() => {
    console.log('Connected to MongoDB');
  })
  .catch((e) => {
    _6ee‍.g.console.error(e);
  });

const app = new Koa();
const router = new Router();

router.use('/api', api.routes());

app.use(bodyParser());
app.use(jwtMiddleware);

app.use(router.routes()).use(router.allowedMethods());

const port = PORT || 4000;
app.listen(port, () => {
  _6ee‍.g.console.log('Listening to port %d', port);
});
